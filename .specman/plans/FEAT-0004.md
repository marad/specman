# Sync plan — FEAT-0004 Agent sync workflow

Started: 2026-04-28T13:14:49.578Z
Snapshot state: new
Drift summary: 25 added (whole spec)

> **Plan note:** FEAT-0004 was implemented before SpecMan tracked itself. This sync establishes the first snapshot. Most ACs (1–17, 19–21) are already satisfied by existing code and reference it for the audit trail; AC-18 needs a one-line wording fix; AC-22, AC-23, AC-24, AC-25 require new code (`seal --initial` and `sync --dry-run`).

## AC-1 (added): Given a drifted spec, `specman sync <id>` produces a plan in which each entry references at least one AC id from the spec.

Approach: Already implemented. `generatePlan` (src/plan.ts) emits one section per `DriftEntry`, each titled with its AC id.

Files: src/plan.ts (existing)

## AC-2 (added): Given no drift in the target spec(s), `specman sync` is a no-op and exits successfully with a message naming the in-sync specs.

Approach: Already implemented. `syncOne` (src/sync.ts:560-567) returns `in-sync` outcome when status is in-sync.

Files: src/sync.ts (existing)

## AC-3 (added): Given a successful execution, the snapshot file (per FEAT-0003) and the plan file (per FEAT-0009) are both written as part of a single commit — the snapshot commit — that is the last commit of the sync invocation. No further code changes land after the snapshot commit within the same sync.

Approach: Already implemented. `writeSnapshotCommit` (src/sync.ts:381) stages snapshot + plan in one commit `[specman] seal <id> (implemented snapshot @ sync)`.

Files: src/sync.ts (existing)

## AC-4 (added): Given a failed execution — agent error, verification failure, or explicit user abort — no snapshot commit is created. Intermediate code commits authored by the agent before the failure remain in history with their `Spec:` trailers intact, and the spec continues to report as drifted until a subsequent sync completes.

Approach: Already implemented. Snapshot commit is only written after verification + trailer check pass; failure paths exit before reaching `writeSnapshotCommit`.

Files: src/sync.ts (existing)

## AC-5 (added): Given a spec with no existing snapshot (status `new` per FEAT-0003), the agent receives the full current spec as the delta (not a diff against nothing).

Approach: Already implemented. `loadDriftSet` with state `new` returns the whole spec as drift; `generatePlan` formats it.

Files: src/plan.ts, src/sync.ts (existing)

## AC-6 (added): Given a plan produced in step 4, the user is shown the plan before execution and must choose one of `approve`, `re-plan`, or `abort` (per FEAT-0009); execution begins only on approval.

Approach: Out of SpecMan's tool boundary — this is the agent/IDE wrapper's responsibility (FEAT-0009 plan format makes the plan readable; the human-agent loop happens outside the CLI). No code change required for this AC; SpecMan only writes the plan and exits.

Files: (none — outside tool scope)

## AC-7 (added): Given `specman sync` with no id argument, every drifted or new spec in the repo is processed in dependency order (using `depends_on`), one at a time. If a spec's sync fails (verification failure, agent error, or user abort), that spec is skipped and all specs that transitively depend on it are also skipped with a message naming the dependency chain. Independent specs continue to be processed.

Approach: Already implemented. `syncAll` (src/sync.ts:647) + `topologicalSort` + `findTransitiveDependents`.

Files: src/sync.ts (existing)

## AC-8 (added): Given any code commit produced by the agent during sync, its commit message contains a `Spec: <FEAT-ID>/<AC-ID>` trailer identifying at least one AC the commit addresses.

Approach: Convention enforced via AC-21 trailer check; the agent (not SpecMan) authors trailers. No code in SpecMan generates them.

Files: (none — agent responsibility)

## AC-9 (added): Given the snapshot commit produced by a successful sync, its commit message is authored by SpecMan (not the agent) and follows a stable template identifying the sealed spec.

Approach: Already implemented. `writeSnapshotCommit` (src/sync.ts:424) uses fixed template `[specman] seal <id> (implemented snapshot @ sync)`.

Files: src/sync.ts (existing)

## AC-10 (added): Given execution reaches verification, each command from the plan's `## Verification` section runs sequentially in the listed order, from the repository root, using the user's shell environment as inherited at sync invocation.

Approach: Already implemented. `runVerification` (src/sync.ts:305) iterates plan commands sequentially via `runShellCommand`.

Files: src/sync.ts (existing)

## AC-11 (added): Given a verification command exits non-zero, no further verification commands run, no snapshot commit is created, and the failing command's stdout, stderr, exit code, and the command string itself are surfaced to the user.

Approach: Already implemented. `runVerification` short-circuits on non-zero; `formatSyncResult` surfaces the failure details.

Files: src/sync.ts (existing)

## AC-12 (added): Given a verification command leaves new uncommitted changes in the working tree (detected via `git status --porcelain` after the command completes), verification is treated as failed; no snapshot commit is created.

Approach: Already implemented. Per-command `getDirtyPaths` check inside `runVerification`.

Files: src/sync.ts (existing)

## AC-13 (added): Given every verification command exits zero and the working tree remains clean throughout, SpecMan proceeds to write the snapshot commit.

Approach: Already implemented. `runVerification` returns `passed: true` only when all commands exit 0 and tree stays clean.

Files: src/sync.ts (existing)

## AC-14 (added): Given `specman sync <ID>` invoked while the working tree has uncommitted changes outside of `.specman/plans/<ID>.md`, sync exits with an error naming the dirty paths before producing a plan or invoking the agent. An uncommitted `.specman/plans/<ID>.md` alone is permitted and triggers the resume flow (FEAT-0009).

Approach: Already implemented. CLI sync case (cli.ts:258) calls `checkWorkingTree` allowing the plan path; resume flow handled in `syncOne` via `planHasUncommittedChanges`.

Files: cli.ts, src/sync.ts (existing)

## AC-15 (added): Given `specman sync` (no ID) invoked while the working tree has uncommitted changes outside of `.specman/plans/`, sync exits with an error naming the dirty paths. Uncommitted plan files for any spec in the sync scope are permitted; each triggers its own resume flow when that spec's turn arrives.

Approach: Already implemented. CLI sync case (cli.ts:289) filters out `.specman/plans/` from the dirty list.

Files: cli.ts, src/sync.ts (existing)

## AC-16 (added): Given `specman seal <FEAT-ID>` where the spec is drifted and no acceptance criteria have changed relative to the snapshot, the snapshot is updated and a single commit is created.

Approach: Already implemented. `seal` (src/sync.ts:723) calls `writeSnapshotCommit` for editorial drift.

Files: src/sync.ts (existing)

## AC-17 (added): Given `specman seal <FEAT-ID>` where the spec has AC-level drift (ACs added, removed, or changed), the command exits non-zero with an error directing the user to `specman sync`.

Approach: Already implemented. `seal` checks `driftSet.length > 0` and exits with "use sync" error.

Files: src/sync.ts (existing)

## AC-18 (added): Given `specman seal <FEAT-ID>` where the spec is `in-sync`, the command exits non-zero with a message saying there is nothing to seal. Given `specman seal <FEAT-ID>` where the spec is `new` and `--initial` is not passed, the command exits non-zero directing the user to use `specman sync` or `specman seal --initial`.

Approach: First half implemented (in-sync rejection). Second half drifts: current message says only "Use 'specman sync ${featId}' for initial implementation." Update wording to also mention `specman seal --initial ${featId}`.

Files: src/sync.ts (modify error message at line 741)

## AC-19 (added): Given `specman sync <ID>` where the spec is drifted but no acceptance criteria have been added, removed, or changed relative to the snapshot, sync exits without producing a plan and prints a message directing the user to update ACs or run `specman seal <ID>`.

Approach: Already implemented. `syncOne` returns `no-ac-drift` outcome (src/sync.ts:584) directing to seal.

Files: src/sync.ts (existing)

## AC-20 (added): Given `specman seal <FEAT-ID>` invoked while the working tree has uncommitted changes, the command exits non-zero with an error naming the dirty paths and does not modify the snapshot.

Approach: Already implemented. `seal` (src/sync.ts:753) calls `checkWorkingTree([])` and refuses on dirt.

Files: src/sync.ts (existing)

## AC-21 (added): Given execution completes and new commits exist since sync started, SpecMan checks that every commit carries at least one `Spec: <FEAT-ID>/<AC-ID>` trailer matching the synced spec. If any commit lacks a matching trailer, sync fails before verification, surfacing the offending commit hash and message.

Approach: Already implemented. `checkTrailers` (src/sync.ts:228) returns offenders; `formatSyncResult` surfaces hash + first message line.

Files: src/sync.ts (existing)

## AC-22 (added): Given `specman seal --initial <FEAT-ID>` where the spec is `new`, the snapshot is created and a single commit is written. The spec transitions from `new` to `in-sync`.

Approach (NEW CODE):
1. In `cli.ts` `seal` case, parse `--initial` flag from `args.slice(1)` and pass it to seal logic.
2. In `src/sync.ts`, change `seal(root, featId)` signature to `seal(root, featId, opts?: { initial?: boolean })`. When `initial === true`:
   - Find spec via `walkSpecFiles`; error if missing.
   - Compute status via `detectDrift`; require status === "new" (AC-23 covers the negative case).
   - Check working tree (reuse existing `checkWorkingTree([])`).
   - Call `writeSnapshotCommit(root, featId, specRelPath)` — same primitive as editorial seal; the commit message can keep the existing template (`[specman] seal <id> (implemented snapshot @ sync)`) since it accurately describes "snapshot at first seal."
   - Return `outcome: "sealed"` with message `<id>: sealed (initial snapshot created).`
3. Update CLI help block for `seal` to document `--initial`.

Files: cli.ts, src/sync.ts

## AC-23 (added): Given `specman seal --initial <FEAT-ID>` where the spec already has a snapshot (status `drifted` or `in-sync`), the command exits non-zero with an error saying `--initial` is only for specs with no snapshot.

Approach (NEW CODE): In the `seal({initial: true})` branch above, when `status !== "new"`, return `outcome: "error"` with message: `--initial is only for specs with no snapshot; <id> is currently <status>. Use 'specman seal <id>' for editorial changes or 'specman sync <id>' for AC drift.`

Files: src/sync.ts (same change as AC-22)

## AC-24 (added): Given `specman sync --dry-run`, each spec that would be synced is listed with its FEAT-ID, status, and drift set (ACs added/modified/removed). No plan files are written. Exit code is zero.

Approach (NEW CODE):
1. In `cli.ts` `sync` case (multi-spec branch), parse `--dry-run` flag. When set, skip the working-tree check (dry-run is read-only), skip `syncAll`, instead:
   - Walk spec files, compute status per spec via `detectDrift`.
   - For each `new` or `drifted` spec, compute drift set via `loadDriftSet` and print one line: `<id> <status>: N added, M modified, K removed`. (Use `DriftEntry.type` counts.) For zero-drift, skip.
   - Order by dependency (reuse `topologicalSort`) for consistent output.
   - Exit 0.
2. Implement helper `dryRunReport(root): DryRunResult` in `src/sync.ts` returning per-spec drift summary; `cli.ts` formats it.

Files: cli.ts, src/sync.ts

## AC-25 (added): Given `specman sync --dry-run <FEAT-ID>` for a single spec, only that spec's drift is shown.

Approach (NEW CODE): Same handler as AC-24, but when a `FEAT-ID` is also passed, filter to that spec only. If it's `in-sync`, print `<id> in-sync — nothing to do.` Exit 0 either way.

Files: cli.ts, src/sync.ts (same change as AC-24)

## Verification

- `cd /Users/marcin.radoszewski/dev/specman.worktrees/feat-0004-seal-initial && deno test --allow-all`
- `cd /Users/marcin.radoszewski/dev/specman.worktrees/feat-0004-seal-initial && deno run --allow-all cli.ts seal --help | grep -- --initial`
- `cd /Users/marcin.radoszewski/dev/specman.worktrees/feat-0004-seal-initial && deno run --allow-all cli.ts sync --help | grep -- --dry-run`
